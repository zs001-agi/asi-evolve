# Auto-generated by Wutong ASI agent
import numpy as np

class GeneticAlgorithm:
    def __init__(self, population_size, mutation_rate):
        self.population = []
        self.mutation_rate = mutation_rate
        self.population_size = population_size

    def initialize_population(self):
        for _ in range(self.population_size):
            individual = np.random.rand(8)  # Assuming each strategy is a random number between 0 and 1
            self.population.append(individual)

    def evolve(self, generations):
        for gen in range(generations):
            new_population = []
            for _ in range(self.population_size):
                parent1 = np.random.choice(self.population, size=2, replace=False)
                child = self.crossover(parent1[0], parent1[1])
                child = self.mutate(child)
                new_population.append(child)
            self.population = new_population

    def crossover(self, parent1, parent2):
        # Implement a simple crossover operator
        return (parent1 + parent2) / 2

    def mutate(self, individual):
        # Implement a simple mutation operator
        for i in range(len(individual)):
            if np.random.rand() < self.mutation_rate:
                individual[i] = 1 - individual[i]
        return individual

# Example usage
ga = GeneticAlgorithm(50, 0.1)
ga.initialize_population()
ga.evolve(10)

print("Final population:", ga.population)