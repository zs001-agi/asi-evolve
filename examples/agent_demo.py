# Auto-generated by Wutong ASI agent
import numpy as np
from random import random

class Gene:
    def __init__(self, size):
        self.values = [random() for _ in range(size)]

    def mutate(self):
        for i in range(len(self.values)):
            if random() < 0.1:  # Change each gene with a probability of 0.1
                self.values[i] = random()

class Population:
    def __init__(self, size, gene_size):
        self.genes = [Gene(gene_size) for _ in range(size)]

    def evolve(self, mutation_rate):
        for gene in self.genes:
            gene.mutate()
        # Here you can add genetic operators like crossover, selection, etc.

# Example usage
population_size = 100
gene_size = 20

population = Population(population_size, gene_size)
population.evolve(0.1)

print("Evolved genes:")
for gene in population.genes:
    print(gene.values)